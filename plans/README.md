# Implementation Plans

Generated by the **improve** skill on 2026-06-10. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add DB indexes on `Chat.userId` and `Message_v2.chatId` | P1 | S | — | DONE ✅ |
| 002  | Extract `hasRenderableContent` to shared module | P1 | S | — | TODO |
| 003  | Remove `"use client"` from `lib/learning-state-machine.ts` | P1 | S | — | TODO |
| 004  | Exclude `.superpowers/` from lint checks | P1 | S | — | TODO |
| 005  | Upgrade `@types/react` to match React 19 | P1 | S | — | TODO |
| 006  | Fix guest email collision with random suffix | P1 | S | — | TODO |
| 007  | Type-safe `streamText` config — remove `as any` | P1 | S | — | TODO |
| 008  | Replace `new Function` with sandboxed evaluator | P1 | M | — | TODO |
| 009  | Parallelise adaptive engine DB queries | P2 | M | 001 | DONE ✅ |
| 010  | Regenerate Drizzle snapshots for 0008–0010 | P2 | M | — | TODO |

## Dependency notes

- 009 (parallel queries) benefits from 001 (indexes) since the parallel queries will
  contend less with proper indexes, but does not strictly require it.

## Findings considered and rejected

- **Guest free-chat limit unenforced**: not a code bug (the limit applies to
  Challenge Mode, not free chat; the product doc tags it 🚧). Worth a direction
  discussion, not a fix plan.
- **31 source files excluded from lint**: intentional to avoid a11y noise in UI
  primitives. A future re-evaluation should suppress individual rules, not entire
  files — filed as a lower-priority DX improvement.
- **Dormant artifact system**: large removal task (~25 files, many deps) relative
  to impact (dead code that doesn't affect runtime paths). Defers to a dedicated
  cleanup phase.
- **Archetype coverage gap (7/13 missions)**: content authoring task, not a
  codebase fix. Tracked in AGENTS.md Future Scope.
